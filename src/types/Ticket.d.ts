interface TicketType {
    id: number;
    price: number,
    amount: number,
    domainWhiteList: string,
}

interface TicketType {
    id: number;
    price: number,
    amount: number,
    domainWhiteList: string,
}